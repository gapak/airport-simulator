

export var vehicles = {};

vehicles = {
    aircraft: {
        key:         'aircraft',
        onTick:      (state, params = {}) => {
            // some code
            return state;
        },

    },

    train: {
        key:         'train',
        onTick:      (state, params = {}) => {
            // some code
            return state;
        },

    },

    taxi: {
        key:         'taxi',
        onTick:      (state, params = {}) => {
            // some code
            return state;
        },

    },

};